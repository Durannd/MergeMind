package com.ricael.mergemind.dto.request;

import com.ricael.mergemind.domain.Project;
import com.ricael.mergemind.domain.enums.Stacks;

import java.util.List;

public record RoleRefRequest(
        Long id
        ) {
}
